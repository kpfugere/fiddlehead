---
import Base from '../layouts/Base.astro';
import Footer from '../components/Footer.astro';

const features = [
  {
    label: 'Pricing',
    fiddlehead: 'Free / Open source',
    granola: '$18/mo',
    otter: '$8–17/mo',
    fireflies: '$10–18/mo',
    fiddleheadWin: true,
  },
  {
    label: 'Subscription required',
    fiddlehead: 'No',
    granola: 'Yes',
    otter: 'Yes',
    fireflies: 'Yes',
    fiddleheadWin: true,
  },
  {
    label: 'Meeting bot joins call',
    fiddlehead: 'No',
    granola: 'No',
    otter: 'Yes',
    fireflies: 'Yes',
    fiddleheadWin: true,
  },
  {
    label: 'Speaker diarization',
    fiddlehead: 'Named speakers',
    granola: 'No',
    otter: 'Business+ only',
    fireflies: 'Yes',
    fiddleheadWin: true,
  },
  {
    label: 'Output format',
    fiddlehead: 'Markdown + YAML',
    granola: 'Proprietary',
    otter: 'Proprietary',
    fireflies: 'Proprietary',
    fiddleheadWin: true,
  },
  {
    label: 'Data storage',
    fiddlehead: 'Local (your Mac)',
    granola: 'Cloud',
    otter: 'Cloud',
    fireflies: 'Cloud',
    fiddleheadWin: true,
  },
  {
    label: 'AI-agent ready',
    fiddlehead: 'Yes (YAML frontmatter)',
    granola: 'MCP only',
    otter: 'No',
    fireflies: 'No',
    fiddleheadWin: true,
  },
  {
    label: 'Platform',
    fiddlehead: 'macOS',
    granola: 'macOS, Windows, Web',
    otter: 'Web, iOS, Android',
    fireflies: 'Web, iOS, Android',
  },
  {
    label: 'File ownership',
    fiddlehead: 'Plain .md files',
    granola: 'Cloud-locked',
    otter: 'Cloud-locked',
    fireflies: 'Cloud-locked',
    fiddleheadWin: true,
  },
  {
    label: 'Free tier',
    fiddlehead: 'Unlimited',
    granola: '25 meetings',
    otter: '300 min/mo',
    fireflies: '800 min storage',
    fiddleheadWin: true,
  },
  {
    label: 'Language support',
    fiddlehead: 'English',
    granola: '10+',
    otter: '3 languages',
    fireflies: '100+',
  },
];

const switchReasons = [
  {
    title: 'you want to own your meeting notes',
    desc: 'fiddlehead saves plain markdown files on your mac. no cloud lock-in, no export button needed — they\'re already yours.',
  },
  {
    title: 'you\'re tired of paying monthly',
    desc: 'free and open source instead of $216+/year for granola or $96–204/year for otter. no subscription, no renewal, no vendor.',
  },
  {
    title: 'you use ai agents',
    desc: 'structured yaml frontmatter means claude, cursor, or any ai agent can read and query your meeting history directly.',
  },
  {
    title: 'you care about privacy',
    desc: 'audio is transcribed and discarded. notes never leave your mac. no account required, no data on our servers.',
  },
  {
    title: 'you need speaker names',
    desc: 'fiddlehead uses speaker diarization with real names in every transcript. granola doesn\'t identify speakers at all.',
  },
];

const differentiators = [
  {
    title: 'free and open source',
    desc: 'mit licensed. no subscription, no license key, no vendor. fork it, ship it, run it forever.',
  },
  {
    title: 'your files, your machine',
    desc: 'plain markdown files saved locally. no export button because there\'s nothing to export — they\'re already files on your mac.',
  },
  {
    title: 'ai-agent native',
    desc: 'structured YAML frontmatter with date, speakers, topics, and tags. your AI agents can query meeting notes directly.',
  },
  {
    title: 'named speakers',
    desc: 'real names in every transcript — not "Me" and "Them." granola doesn\'t even do speaker diarization.',
  },
];
---

<Base
  title="fiddlehead vs granola — private ai meeting transcription alternative"
  description="compare fiddlehead to granola, otter.ai, and fireflies. a private, local-first meeting note taker with ai meeting transcription, no subscription, and ai-agent ready markdown output."
>
  <nav class="page-nav">
    <div class="page-nav-inner">
      <a href="/" class="page-nav-logo" aria-label="Back to home">
        <img src="/images/frog-icon.svg" alt="" width="28" height="21" />
        <span class="page-nav-name">fiddlehead</span>
      </a>
    </div>
  </nav>

  <main>
    <section class="compare-hero">
      <div class="compare-hero-content hero-enter hero-enter-1">
        <h1 class="heading-lg">fiddlehead vs granola, otter, and fireflies</h1>
        <p class="subhead compare-sub">
          a local-first, private alternative to cloud meeting note takers. no subscription, no meeting bot, ai-agent ready.
        </p>
      </div>
    </section>

    <section class="compare-intro">
      <p class="compare-intro-text hero-enter hero-enter-2">
        fiddlehead is a local-first ai meeting note taker for macos and a privacy-focused alternative to granola. it records meeting audio, transcribes with speaker diarization, and saves structured markdown notes on your mac — not in the cloud. if you're looking for an ai meeting transcription app that doesn't require a subscription, a meeting bot, or an account, here's how fiddlehead compares.
      </p>
    </section>

    <section class="compare-table-section">
      <div class="table-scroll hero-enter hero-enter-2">
        <table class="compare-table">
          <thead>
            <tr>
              <th class="col-feature"></th>
              <th class="col-fiddlehead">fiddlehead</th>
              <th class="col-competitor">granola</th>
              <th class="col-competitor">otter.ai</th>
              <th class="col-competitor">fireflies</th>
            </tr>
          </thead>
          <tbody>
            {features.map(f => (
              <tr>
                <td class="col-feature">{f.label}</td>
                <td class:list={['col-fiddlehead', { 'cell-win': f.fiddleheadWin }]}>{f.fiddlehead}</td>
                <td class="col-competitor">{f.granola}</td>
                <td class="col-competitor">{f.otter}</td>
                <td class="col-competitor">{f.fireflies}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>

    <section class="compare-diff">
      <h2 class="heading-md reveal">why fiddlehead</h2>
      <div class="diff-grid reveal reveal-delay-1">
        {differentiators.map(d => (
          <div class="diff-card">
            <span class="glyph glyph-saved">✓</span>
            <div>
              <strong class="diff-title">{d.title}</strong>
              <p class="diff-desc">{d.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <section class="compare-switch">
      <h2 class="heading-md reveal">choose fiddlehead if</h2>
      <div class="diff-grid reveal reveal-delay-1">
        {switchReasons.map(r => (
          <div class="diff-card">
            <span class="glyph glyph-saved">→</span>
            <div>
              <strong class="diff-title">{r.title}</strong>
              <p class="diff-desc">{r.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <section class="compare-cta reveal">
      <a href="/" class="btn-primary">download for macos</a>
      <p class="cta-note">free and open source — bring your own api key, no account required.</p>
    </section>
  </main>

  <Footer />
</Base>

<style>
  /* nav */
  .page-nav {
    border-bottom: 1px solid var(--border);
    padding: 0 var(--space-xl);
  }
  .page-nav-inner {
    max-width: 1080px;
    margin: 0 auto;
    height: 52px;
    display: flex;
    align-items: center;
  }
  .page-nav-logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--text-primary);
  }
  .page-nav-logo:hover {
    color: var(--accent);
  }
  .page-nav-name {
    font-size: 14px;
    font-weight: 500;
  }

  /* hero */
  .compare-hero {
    padding: var(--space-3xl) var(--space-xl) var(--space-2xl);
    text-align: center;
    display: flex;
    justify-content: center;
  }
  .compare-hero-content {
    max-width: 640px;
  }
  .compare-sub {
    margin-top: var(--space-lg);
    margin-left: auto;
    margin-right: auto;
  }

  /* intro */
  .compare-intro {
    max-width: 720px;
    margin: 0 auto;
    padding: 0 var(--space-xl) var(--space-2xl);
    text-align: center;
  }
  .compare-intro-text {
    font-size: 15px;
    color: var(--text-secondary);
    line-height: 1.7;
  }

  /* table */
  .compare-table-section {
    max-width: 1080px;
    margin: 0 auto;
    padding: 0 var(--space-xl) var(--space-3xl);
  }
  .table-scroll {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
  }
  .compare-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 14px;
    min-width: 820px;
  }
  .compare-table thead {
    border-bottom: 1px solid var(--border);
  }
  .compare-table th {
    padding: 12px 16px;
    font-size: 12px;
    font-weight: 500;
    text-align: left;
    letter-spacing: 0.04em;
  }
  .compare-table td {
    padding: 10px 16px;
    border-top: 1px solid var(--border);
    line-height: 1.5;
  }
  .col-feature {
    color: var(--text-primary);
    font-weight: 500;
    white-space: nowrap;
  }
  .col-fiddlehead {
    color: var(--accent);
    background: rgba(208, 165, 83, 0.04);
  }
  th.col-fiddlehead {
    color: var(--accent);
  }
  .col-competitor {
    color: var(--text-secondary);
    opacity: 0.7;
  }
  th.col-competitor {
    opacity: 0.5;
  }
  .cell-win {
    font-weight: 500;
  }

  /* differentiators */
  .compare-diff {
    max-width: 820px;
    margin: 0 auto;
    padding: 0 var(--space-xl) var(--space-3xl);
  }
  .compare-diff .heading-md {
    text-align: center;
    margin-bottom: var(--space-xl);
  }
  .diff-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg);
  }
  .diff-card {
    display: flex;
    gap: var(--space-md);
    padding: var(--space-xl);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--surface);
  }
  .diff-card .glyph {
    flex-shrink: 0;
    font-size: 14px;
    margin-top: 2px;
  }
  .diff-title {
    color: var(--text-primary);
    font-weight: 500;
    font-size: 15px;
    display: block;
    margin-bottom: 4px;
  }
  .diff-desc {
    color: var(--text-secondary);
    font-size: 13px;
    line-height: 1.65;
  }

  /* switch */
  .compare-switch {
    max-width: 820px;
    margin: 0 auto;
    padding: 0 var(--space-xl) var(--space-3xl);
  }
  .compare-switch .heading-md {
    text-align: center;
    margin-bottom: var(--space-xl);
  }

  /* cta */
  .compare-cta {
    text-align: center;
    padding: 0 var(--space-xl) var(--space-3xl);
  }
  .cta-note {
    margin-top: var(--space-md);
    font-size: 13px;
    color: var(--text-secondary);
  }

  /* responsive */
  @media (max-width: 768px) {
    .diff-grid {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 640px) {
    .compare-hero {
      padding: var(--space-2xl) var(--space-lg) var(--space-xl);
    }
    .compare-table-section {
      padding: 0 var(--space-lg) var(--space-2xl);
    }
    .compare-diff {
      padding: 0 var(--space-lg) var(--space-2xl);
    }
    .compare-cta {
      padding: 0 var(--space-lg) var(--space-2xl);
    }
  }
</style>
